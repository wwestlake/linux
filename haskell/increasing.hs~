

increasing :: (Ord a) => [a] -> Bool
increasing xs = if xs == []
	      	then true
		else if tail xs == []
		     then true
		     else if head xs <= head (tail xs)
		     	  then increasing (tail xs)
			  else false
